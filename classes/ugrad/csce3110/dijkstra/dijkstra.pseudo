// Dijkstra's Algorithm

Dijkstra (G, w, s)
   Initialize_single_source(G, s)
   S := Null Set
   Q := V[G]
   while Q != Null
      do u := Extract-min(q)
         S:=S Union {u}
            do Relax(u, v, w)
         for each vertex v element of Adj[u]